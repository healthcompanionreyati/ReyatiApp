import RecoveryScreen from "@/app/components/RecoveryScreen";

export default function NotFound() {
  return <RecoveryScreen title="We could not find that page" message="The address may be outdated or the workspace may have moved. Use a safe destination below to continue."/>;
}
