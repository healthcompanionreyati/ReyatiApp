"use client";import{FormEvent,useCallback,useEffect,useState}from"react";import{useReyatiLocale}from"@/app/components/useReyatiLocale";import styles from"@/app/appointment-journey.module.css";type Profile={id:string;exchangeType:string;dataClassification:string;transportProtection:string;messageIntegrity:string;replayProtection:string;minimizationProfile:string;loggingProfile:string;evidenceState:string;status:string;version:number};type D={profiles:Profile[];notice:string};const o=(a:string[])=>a.map(v=><option key={v} value={v}>{v.replaceAll("_"," ")}</option>);export default function Page(){const[lang,setLang]=useReyatiLocale(),ar=lang==="ar",[data,setData]=useState<D|null>(null),[error,setError]=useState(""),[busy,setBusy]=useState(false);const load=useCallback(async()=>{setError("");try{const r=await fetch("/api/admin/integration-payload-security",{cache:"no-store"}),p=await r.json().catch(()=>({}))as{data?:D;message?:string};if(!r.ok||!p.data)throw Error(p.message??"Unable to load payload security");setData(p.data)}catch(e){setError(e instanceof Error?e.message:"Unavailable")}},[]);useEffect(()=>{queueMicrotask(()=>void load())},[load]);async function send(b:Record<string,unknown>){setBusy(true);setError("");try{const r=await fetch("/api/admin/integration-payload-security",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(b)}),p=await r.json().catch(()=>({}))as{message?:string};if(!r.ok)throw Error(p.message??"Unable to save");await load()}catch(e){setError(e instanceof Error?e.message:"Unable to save")}finally{setBusy(false)}}async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=e.currentTarget;await send({action:"create",...Object.fromEntries(new FormData(f).entries())});f.reset()}return <main className={styles.shell} dir={ar?"rtl":"ltr"}><header className={styles.top}><a href="/admin"><img src="/brand/reyati-logo.svg" alt="Reyati"/></a><button className={styles.lang} onClick={()=>setLang(ar?"en":"ar")}>{ar?"EN":"العربية"}</button></header><section className={styles.hero}><span>PAYLOAD PROTECTION CONTROL</span><h1>{ar?"حوكمة أمن حمولة التكامل":"Integration payload security governance"}</h1><p>{ar?"ملفات حماية ومراجعة مستقلة دون معالجة أو نقل أي بيانات حقيقية.":"Protection profiles and independent review without processing or transmitting real data."}</p></section><section className={styles.boundary}><b>{ar?"لا تدخل بيانات أو معرفات أو مفاتيح":"Never enter payloads, identities, or keys"}</b><span>{ar?"لا تشفير أو توقيع أو تحويل أو نقل أو وصول إلى المفاتيح.":"No encryption, decryption, signing, transformation, transmission, key access, or external contact."}</span></section>{error&&<div className={styles.error}>{error}<button onClick={()=>void load()}>Retry</button></div>}<div className={styles.grid}><form className={styles.panel} onSubmit={e=>void submit(e)}><div className={styles.panelHead}><h2>{ar?"ملف حماية":"Protection profile"}</h2></div><label>Opaque profile reference<input name="profileReference" required maxLength={160} autoComplete="off"/></label><label>Exchange type<select name="exchangeType">{o(["rest_request_response","webhook_event","batch_file","document_exchange","event_stream_review"])}</select></label><label>Data classification<select name="dataClassification">{o(["operational_metadata","personal_data","health_data_minimum","payment_reference","security_event"])}</select></label><label>Transport protection<select name="transportProtection">{o(["tls_1_3_required","mtls_required","managed_sftp_required","private_link_review"])}</select></label><label>Message integrity<select name="messageIntegrity">{o(["https_transport_only","detached_signature_required","webhook_signature_required","file_checksum_required"])}</select></label><label>Replay protection<select name="replayProtection">{o(["timestamp_nonce_required","idempotency_key_required","sequence_control_required","not_applicable_review"])}</select></label><label>Minimization<select name="minimizationProfile">{o(["identifier_only","minimum_clinical_set","reference_only","aggregate_only"])}</select></label><label>Logging<select name="loggingProfile">{o(["metadata_only","coded_outcome_only","hashed_reference_only","no_payload_logging"])}</select></label><label>Evidence<select name="evidenceState">{o(["not_started","partial","complete_not_applied"])}</select></label><button disabled={busy}>Create profile</button></form><section className={styles.panel}><div className={styles.panelHead}><h2>{ar?"ملفات الحماية":"Protection profiles"}</h2><span>{data?.profiles.length??0}</span></div><p>{data?.notice}</p>{!data?.profiles.length?<div className={styles.empty}><b>No profiles yet</b><span>Create an evidence-only protection profile.</span></div>:<div className={styles.list}>{data.profiles.map(p=><article key={p.id}><div className={styles.cardTop}><b>{p.exchangeType.replaceAll("_"," ")}</b><span>{p.status}</span></div><p>{p.dataClassification.replaceAll("_"," ")} · {p.transportProtection.replaceAll("_"," ")} · {p.messageIntegrity.replaceAll("_"," ")} · {p.replayProtection.replaceAll("_"," ")} · {p.minimizationProfile.replaceAll("_"," ")} · {p.loggingProfile.replaceAll("_"," ")}</p>{p.status==="draft"&&<button disabled={busy} onClick={()=>void send({action:"submit",profileId:p.id,version:p.version})}>Submit for review</button>}{p.status==="submitted"&&<div><button disabled={busy} onClick={()=>void send({action:"review",decisionCode:"evidence_reviewed",profileId:p.id,version:p.version})}>Review evidence</button><button disabled={busy} onClick={()=>void send({action:"review",decisionCode:"returned",profileId:p.id,version:p.version})}>Return</button></div>}</article>)}</div>}</section></div></main>}
