"use client";

import Image from "next/image";
import { useCallback, useEffect, useState } from "react";
import { useReyatiLocale } from "@/app/components/useReyatiLocale";
import styles from "./payment-receipts.module.css";

type CreditNote={id:string;creditNoteNumber:string;amountMinor:number;currency:string;reasonCode:string;issuedAt:string};
type Receipt={id:string;receiptNumber:string;providerName:string;facilityName:string|null;appointmentStartedAt:string;careMode:string;amountMinor:number;currency:string;issuedAt:string;creditNotes:CreditNote[]};
function money(value:number,currency:string,ar:boolean){return new Intl.NumberFormat(ar?"ar-QA":"en-QA",{style:"currency",currency:currency.toUpperCase()}).format(value/100)}
function stamp(value:string,ar:boolean){return new Intl.DateTimeFormat(ar?"ar-QA":"en-QA",{dateStyle:"long",timeStyle:"short",timeZone:"Asia/Qatar"}).format(new Date(value))}

export default function PaymentReceipts(){
  const [lang,setLang]=useReyatiLocale();const ar=lang==="ar";
  const [receipts,setReceipts]=useState<Receipt[]>([]),[selected,setSelected]=useState<Receipt|null>(null),[delegated,setDelegated]=useState(false),[loading,setLoading]=useState(true),[error,setError]=useState("");
  const load=useCallback(async()=>{setError("");try{const subject=new URLSearchParams(location.search).get("subjectUserId"),url=subject?`/api/patient/payment-receipts?subjectUserId=${encodeURIComponent(subject)}`:"/api/patient/payment-receipts";const response=await fetch(url,{cache:"no-store"}),payload=await response.json().catch(()=>({})) as {data?:Receipt[];delegated?:boolean;message?:string};if(response.status===401){location.assign(`/sign-in?redirect_url=${encodeURIComponent(`/payment-receipts${location.search}`)}`);return}if(!response.ok||!payload.data)throw new Error(payload.message||"Receipts are temporarily unavailable");setReceipts(payload.data);setDelegated(payload.delegated===true)}catch(caught){setError(caught instanceof Error?caught.message:"Receipts are temporarily unavailable")}finally{setLoading(false)}},[]);
  useEffect(()=>{void load()},[load]);
  const credited=receipts.reduce((total,item)=>total+item.creditNotes.reduce((sum,note)=>sum+note.amountMinor,0),0);
  return <main className={styles.shell} dir={ar?"rtl":"ltr"} id="main-content"><header className={styles.top}><a href="/"><Image src="/brand/qivaya-logo-primary.png" width={112} height={42} alt="Qivaya"/></a><nav><a href="/appointments">{ar?"المواعيد":"Appointments"}</a><a href="/payments">{ar?"المدفوعات":"Payments"}</a><a className={styles.active} href="/payment-receipts">{ar?"الإيصالات":"Receipts"}</a><a href="/payment-support">{ar?"الدعم":"Support"}</a></nav><button onClick={()=>setLang(ar?"en":"ar")}>{ar?"English":"العربية"}</button></header><section className={styles.hero}><div><p>{ar?"مستندات مالية مملوكة للحساب":"ACCOUNT-OWNED FINANCIAL DOCUMENTS"}</p><h1>{ar?"إيصالات الدفع":"Payment receipts"}</h1><span>{ar?"إثباتات ثابتة يتم إنشاؤها فقط بعد تأكيد موقّع من مزود الدفع.":"Immutable records created only after signed payment-provider confirmation."}</span></div><i>Q</i></section><section className={styles.content}>{delegated&&<div className={styles.delegated}>{ar?"أنت تعرض الإيصالات عبر صلاحية مدفوعات نشطة وقابلة للإلغاء.":"You are viewing receipts through an active, revocable payments permission."}</div>}{error&&<div className={styles.error} role="alert"><span>{error}</span><button onClick={()=>void load()}>{ar?"إعادة المحاولة":"Retry"}</button></div>}<section className={styles.summary}><article><span>✓</span><div><b>{receipts.length}</b><small>{ar?"إيصالات مؤكدة":"Confirmed receipts"}</small></div></article><article><span>↩</span><div><b>{receipts.reduce((n,r)=>n+r.creditNotes.length,0)}</b><small>{ar?"إشعارات دائنة":"Credit notes"}</small></div></article><article><span>Q</span><div><b>{money(credited,"qar",ar)}</b><small>{ar?"قيمة الاسترداد المؤكدة":"Confirmed credited value"}</small></div></article></section><div className={styles.boundary}><span>i</span><p><b>{ar?"إيصال دفع، وليس فاتورة ضريبية.":"Payment receipt, not a tax invoice."}</b>{ar?"لا يعرض هذا السجل بيانات البطاقة ولا ينشئ تسوية أو دفعة لمقدم الخدمة.":"This record contains no card data and creates no settlement or provider payout."}</p></div>{loading?<div className={styles.state}>{ar?"جارٍ تحميل الإيصالات الموثوقة…":"Loading trusted receipts…"}</div>:!receipts.length?<div className={styles.state}><b>{ar?"لا توجد إيصالات دفع بعد":"No payment receipts yet"}</b><span>{ar?"سيظهر الإيصال بعد أن يؤكد مزود الدفع عملية ناجحة موقّعة.":"A receipt will appear after the payment provider signs a successful payment event."}</span><a href="/payments">{ar?"فتح المدفوعات":"Open payments"}</a></div>:<section className={styles.list}><header><div><p>{ar?"سجل الإيصالات":"RECEIPT REGISTER"}</p><h2>{ar?"المستندات المؤكدة":"Confirmed documents"}</h2></div><span>{receipts.length}</span></header>{receipts.map(receipt=><button key={receipt.id} onClick={()=>setSelected(receipt)}><time>{stamp(receipt.issuedAt,ar)}<small>{receipt.receiptNumber}</small></time><div><b>{receipt.providerName}</b><small>{receipt.facilityName|| (ar?"رعاية افتراضية أو موقع غير مسجل":"Virtual care or facility not recorded")}</small></div><span>{receipt.careMode.replaceAll("_"," ")}</span><strong>{money(receipt.amountMinor,receipt.currency,ar)}</strong><em>{receipt.creditNotes.length?`${receipt.creditNotes.length} ${ar?"دائن":"credit"}`:"›"}</em></button>)}</section>}</section>{selected&&<div className={styles.layer} onMouseDown={event=>event.target===event.currentTarget&&setSelected(null)}><article className={styles.receipt}><button className="drawer-close" onClick={()=>setSelected(null)} aria-label={ar?"إغلاق":"Close"}>×</button><Image src="/brand/qivaya-logo-primary.png" width={116} height={44} alt="Qivaya"/><p>{ar?"إيصال دفع مؤكد":"CONFIRMED PAYMENT RECEIPT"}</p><h2>{money(selected.amountMinor,selected.currency,ar)}</h2><span>{selected.receiptNumber}</span><dl><div><dt>{ar?"مقدم الرعاية":"Care provider"}</dt><dd>{selected.providerName}</dd></div><div><dt>{ar?"الموقع":"Location"}</dt><dd>{selected.facilityName||"—"}</dd></div><div><dt>{ar?"موعد الرعاية":"Care appointment"}</dt><dd>{stamp(selected.appointmentStartedAt,ar)}</dd></div><div><dt>{ar?"تاريخ الإصدار":"Issued"}</dt><dd>{stamp(selected.issuedAt,ar)}</dd></div><div><dt>{ar?"طريقة الرعاية":"Care mode"}</dt><dd>{selected.careMode.replaceAll("_"," ")}</dd></div><div><dt>{ar?"الحالة":"Status"}</dt><dd>{ar?"أكدها المزود":"Provider confirmed"}</dd></div></dl>{selected.creditNotes.map(note=><section className={styles.credit} key={note.id}><div><b>{ar?"إشعار دائن للاسترداد":"Refund credit note"}</b><span>{note.creditNoteNumber}</span></div><strong>−{money(note.amountMinor,note.currency,ar)}</strong><time>{stamp(note.issuedAt,ar)}</time></section>)}<div className={styles.note}>{ar?"هذا سجل حالة دفع وليس فاتورة ضريبية أو كشف تسوية.":"This is a payment-status record, not a tax invoice or settlement statement."}</div><footer><button onClick={()=>window.print()}>{ar?"طباعة أو حفظ PDF":"Print or save PDF"}</button><a href="/payment-support">{ar?"الحصول على مساعدة":"Get help"}</a></footer></article></div>}</main>
}
