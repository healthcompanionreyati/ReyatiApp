import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";const r=p=>readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),s=r("db/integration-change-schema.ts"),l=r("lib/integration-change.ts"),a=r("lib/integration-change-api.ts"),p=r("app/admin/integration-change/page.tsx"),g=r("app/admin/integration-change-governance/page.tsx"),f=r("lib/foundation-flags.ts"),c=r("lib/capability-registry.ts"),t=r("app/components/AccessibilitySync.tsx"),d=r("db/index.ts"),n=r("app/admin/page.tsx");
test("change proposals events and rehearsals are durable and indexed",()=>{for(const x of["integration_change_proposals","integration_change_events","integration_change_rehearsals"])assert.match(s,new RegExp(x));for(const x of["uq_integration_change_reference","idx_integration_change_posture","idx_integration_change_events","idx_integration_change_rehearsal"])assert.match(s,new RegExp(x))});
test("opaque references are hashed and change material rejected",()=>{assert.match(l,/crypto\.subtle\.digest\("SHA-256"/);assert.match(s,/changeReferenceHash/);assert.match(l,/never code, configuration, endpoint, identity, contact, or credential material/);assert.doesNotMatch(s,/sourceCode|configurationBody|endpointUrl|contactEmail/)});
test("returned workspace omits change reference hashes",()=>{assert.match(l,/select\(\{id:integrationChangeProposals\.id,changeClass:/);assert.doesNotMatch(l.match(/getChangeWorkspace[\s\S]*?return\{proposals/)?.[0]??"",/changeReferenceHash:/)});
test("classes impacts windows validation rollback and communications are bounded",()=>{for(const x of["mapping_change","breaking_change_review","critical_patient_safety_review","partner_coordinated_window","synthetic_end_to_end","independently_verified","reviewed_not_sent"])assert.match(l,new RegExp(x))});
test("proposal review is optimistic and independent",()=>{assert.match(l,/ne\(integrationChangeProposals\.createdByUserId,u\)/);assert.match(l,/ChangeGovernanceConflictError/);assert.doesNotMatch(l,/\.delete\(/)});
test("all change runtime effects are centrally disabled",()=>{for(const x of["integrationChangeDeploymentExecution","integrationChangeConfigurationMutation","integrationChangeMaintenanceWindowActivation","integrationChangeNoticeDelivery","integrationChangeExternalContact"])assert.equal(f.includes(`${x}: false`),true)});
test("forty six scenario rehearsal has zero effects",()=>{assert.match(l,/scenarioCount:46/);for(const x of["deploymentsStarted:0","configurationsChanged:0","maintenanceWindowsOpened:0","noticesSent:0","externalSystemsContacted:0"])assert.match(l,new RegExp(x))});
test("API is private authenticated rate limited and conflict aware",()=>{assert.match(a,/getOrCreateCurrentUser/);assert.match(a,/enforceWriteRateLimit/);assert.match(a,/private, no-store/);assert.match(a,/rateLimitResponse/)});
test("security auditor receives aggregate posture only",()=>{assert.match(l,/platform_admin",\s*"security_auditor/);assert.match(l,/aggregateOnly:true/);assert.doesNotMatch(g,/changeReferenceHash|createdByUserId|proposalId/)});
test("bilingual operations and governance surfaces recover safely",()=>{assert.match(p,/useReyatiLocale/);assert.match(p,/Retry/);assert.match(p,/Never enter code or configuration/);assert.match(g,/useReyatiLocale/);assert.match(g,/Retry/);assert.match(g,/No sensitive change details/)});
test("schema capability navigation and bilingual titles are integrated",()=>{assert.match(d,/integration-change-schema/);assert.match(d,/\.\.\.integrationChangeSchema/);assert.match(c,/integration_change_window_governance/);for(const x of["/admin/integration-change","/admin/integration-change-governance"])assert.equal(t.split(x).length>=3,true);assert.match(n,/\/admin\/integration-change/)});
