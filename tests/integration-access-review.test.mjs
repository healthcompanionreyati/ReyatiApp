import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";const r=p=>readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),s=r("db/integration-access-review-schema.ts"),l=r("lib/integration-access-review.ts"),a=r("lib/integration-access-review-api.ts"),p=r("app/admin/integration-access-reviews/page.tsx"),g=r("app/admin/integration-access-review-governance/page.tsx"),f=r("lib/foundation-flags.ts"),c=r("lib/capability-registry.ts"),t=r("app/components/AccessibilitySync.tsx"),d=r("db/index.ts"),n=r("app/admin/page.tsx");
test("access reviews events and rehearsals are durable and indexed",()=>{for(const x of["integration_access_reviews","integration_access_review_events","integration_access_review_rehearsals"])assert.match(s,new RegExp(x));for(const x of["uq_integration_access_reference","idx_integration_access_review_posture","idx_integration_access_review_events","idx_integration_access_review_rehearsal"])assert.match(s,new RegExp(x))});
test("opaque references are hashed and credential material rejected",()=>{assert.match(l,/crypto\.subtle\.digest\("SHA-256"/);assert.match(s,/accessReferenceHash/);assert.match(l,/never a credential, token, scope, endpoint, identity, contact, log, or configuration/);assert.doesNotMatch(s,/credentialBody|tokenValue|scopeValue|endpointUrl|contactEmail/)});
test("workspace omits access reference hashes",()=>{assert.match(l,/select\(\{id:integrationAccessReviews\.id,principalClass:/);assert.doesNotMatch(l.match(/getIntegrationAccessReviewWorkspace[\s\S]*?return\{reviews/)?.[0]??"",/accessReferenceHash:/)});
test("principal scope owner cadence expiry segregation and revocation are bounded",()=>{for(const x of["webhook_signer","emergency_account","clinical_exchange_review","privileged_administration","owner_recency_verified","quarterly","independently_recertified","expired_review_required","maker_checker_mapped","synthetic_exception_tested","synthetic_rehearsed"])assert.match(l,new RegExp(x))});
test("recertification is optimistic and independently reviewed",()=>{assert.match(l,/ne\(integrationAccessReviews\.createdByUserId,u\)/);assert.match(l,/IntegrationAccessReviewConflictError/);assert.doesNotMatch(l,/\.delete\(/)});
test("all access runtime effects are centrally disabled",()=>{for(const x of["integrationAccessReviewCredentialIssuance","integrationAccessReviewScopeMutation","integrationAccessReviewAccessRevocation","integrationAccessReviewSessionTermination","integrationAccessReviewExternalContact"])assert.equal(f.includes(`${x}: false`),true)});
test("fifty two scenario rehearsal has zero effects",()=>{assert.match(l,/scenarioCount:52/);for(const x of["credentialsIssued:0","scopesChanged:0","accessRevoked:0","sessionsTerminated:0","externalSystemsContacted:0"])assert.match(l,new RegExp(x))});
test("API is private authenticated rate limited and conflict aware",()=>{assert.match(a,/getOrCreateCurrentUser/);assert.match(a,/enforceWriteRateLimit/);assert.match(a,/private, no-store/);assert.match(a,/rateLimitResponse/)});
test("security auditor receives aggregate posture only",()=>{assert.match(l,/platform_admin",\s*"security_auditor/);assert.match(l,/aggregateOnly:true/);assert.doesNotMatch(g,/accessReferenceHash|createdByUserId|reviewId/)});
test("bilingual operations and governance surfaces recover safely",()=>{assert.match(p,/useReyatiLocale/);assert.match(p,/Retry/);assert.match(p,/Never enter credentials or tokens/);assert.match(g,/useReyatiLocale/);assert.match(g,/Retry/);assert.match(g,/No credentials or identities/)});
test("schema capability navigation and bilingual titles are integrated",()=>{assert.match(d,/integration-access-review-schema/);assert.match(d,/\.\.\.integrationAccessReviewSchema/);assert.match(c,/integration_access_recertification_governance/);for(const x of["/admin/integration-access-reviews","/admin/integration-access-review-governance"])assert.equal(t.split(x).length>=3,true);assert.match(n,/\/admin\/integration-access-reviews/)});
