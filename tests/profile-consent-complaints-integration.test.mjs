import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import test from "node:test";
const read=(path)=>readFileSync(new URL(`../${path}`,import.meta.url),"utf8");
test("three schemas and migration are registered",()=>{const db=read("db/index.ts"),migration=read("drizzle/0064_lethal_bloodstrike.sql");for(const schema of ["personalHealthProfileSchema","consentCenterSchema","complaintsSchema"])assert.match(db,new RegExp(`\\.\\.\\.${schema}`));for(const table of ["personal_health_profiles","consent_policies","complaints"])assert.equal(migration.includes(`CREATE TABLE \`${table}\``),true);assert.match(migration,/PRAGMA optimize;/);});
test("patient and admin navigation and titles expose all modules",()=>{const source=[read("app/page.tsx"),read("app/admin/page.tsx"),read("app/components/AccessibilitySync.tsx")].join("\n");for(const route of ["/health-profile","/admin/health-profile","/consents","/admin/consents","/complaints","/admin/complaints"])assert.match(source,new RegExp(route.replaceAll("/","\\/")));});
test("capabilities and all disabled external boundaries are central",()=>{const registry=read("lib/capability-registry.ts"),flags=read("lib/foundation-flags.ts");for(const id of ["personal_health_profile","purpose_specific_consent_center","complaints_safety_concerns"])assert.match(registry,new RegExp(id));for(const flag of ["healthProfileProviderAccess","healthProfileAutomaticClinicalUse","healthProfileDiagnosisOrRecommendation","healthProfileExternalImport","healthProfileExternalSharing","consentCenterBlanketConsent","consentCenterSilentRenewal","consentCenterProviderOverride","consentCenterExternalSynchronization","consentCenterAutomaticDownstreamActivation","complaintsAutomaticClinicalTriage","complaintsEmergencyDispatch","complaintsExternalRegulatorSubmission","complaintsProviderNotification","complaintsAutomaticCompensationOrRefund","complaintsExternalTicketing"])assert.match(flags,new RegExp(`${flag}: false`));});
