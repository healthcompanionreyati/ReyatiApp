import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";const r=p=>readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),s=r("db/integration-residency-schema.ts"),l=r("lib/integration-residency.ts"),a=r("lib/integration-residency-api.ts"),p=r("app/admin/integration-residency/page.tsx"),g=r("app/admin/integration-residency-governance/page.tsx"),f=r("lib/foundation-flags.ts"),c=r("lib/capability-registry.ts"),t=r("app/components/AccessibilitySync.tsx"),d=r("db/index.ts"),n=r("app/admin/page.tsx");
test("residency profiles events and rehearsals are durable and indexed",()=>{for(const x of["integration_residency_profiles","integration_residency_events","integration_residency_rehearsals"])assert.match(s,new RegExp(x));for(const x of["uq_integration_residency_reference","idx_integration_residency_posture","idx_integration_residency_events","idx_integration_residency_rehearsal"])assert.match(s,new RegExp(x))});
test("opaque references are hashed and record material rejected",()=>{assert.match(l,/crypto\.subtle\.digest\("SHA-256"/);assert.match(s,/flowReferenceHash/);assert.match(l,/never a payload, record, query, endpoint, identity, contact, or credential/);assert.doesNotMatch(s,/payloadBody|recordBody|endpointUrl|contactEmail|legalText/)});
test("workspace omits flow reference hashes",()=>{assert.match(l,/select\(\{id:integrationResidencyProfiles\.id,dataClass:/);assert.doesNotMatch(l.match(/getIntegrationResidencyWorkspace[\s\S]*?return\{profiles/)?.[0]??"",/flowReferenceHash:/)});
test("classification residency transfer processor encryption and exit are bounded",()=>{for(const x of["clinical_exchange","security_audit","qatar_only_intent","multi_region_review","legal_basis_documented","subprocessor_reviewed","both_profiles_reviewed","minimum_necessary_mapped","processor_schedule_aligned","synthetic_exit_rehearsed"])assert.match(l,new RegExp(x))});
test("profile review is optimistic and independent",()=>{assert.match(l,/ne\(integrationResidencyProfiles\.createdByUserId,u\)/);assert.match(l,/IntegrationResidencyConflictError/);assert.doesNotMatch(l,/\.delete\(/)});
test("all residency runtime effects are centrally disabled",()=>{for(const x of["integrationResidencyDataExport","integrationResidencyCrossBorderTransfer","integrationResidencyPayloadInspection","integrationResidencyStorageLocationMutation","integrationResidencyExternalProcessorContact"])assert.equal(f.includes(`${x}: false`),true)});
test("fifty scenario rehearsal has zero effects",()=>{assert.match(l,/scenarioCount:50/);for(const x of["dataExportsStarted:0","crossBorderTransfersStarted:0","payloadsInspected:0","storageLocationsChanged:0","externalProcessorsContacted:0"])assert.match(l,new RegExp(x))});
test("API is private authenticated rate limited and conflict aware",()=>{assert.match(a,/getOrCreateCurrentUser/);assert.match(a,/enforceWriteRateLimit/);assert.match(a,/private, no-store/);assert.match(a,/rateLimitResponse/)});
test("security auditor receives aggregate posture only",()=>{assert.match(l,/platform_admin",\s*"security_auditor/);assert.match(l,/aggregateOnly:true/);assert.doesNotMatch(g,/flowReferenceHash|createdByUserId|profileId/)});
test("bilingual operations and governance surfaces recover safely",()=>{assert.match(p,/useReyatiLocale/);assert.match(p,/Retry/);assert.match(p,/Never enter payloads or records/);assert.match(g,/useReyatiLocale/);assert.match(g,/Retry/);assert.match(g,/No records or transfer details/)});
test("schema capability navigation and bilingual titles are integrated",()=>{assert.match(d,/integration-residency-schema/);assert.match(d,/\.\.\.integrationResidencySchema/);assert.match(c,/integration_data_residency_governance/);for(const x of["/admin/integration-residency","/admin/integration-residency-governance"])assert.equal(t.split(x).length>=3,true);assert.match(n,/\/admin\/integration-residency/)});
