import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";const r=p=>readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),s=r("db/integration-observability-schema.ts"),l=r("lib/integration-observability.ts"),a=r("lib/integration-observability-api.ts"),p=r("app/admin/integration-observability/page.tsx"),g=r("app/admin/integration-observability-governance/page.tsx"),f=r("lib/foundation-flags.ts"),c=r("lib/capability-registry.ts"),t=r("app/components/AccessibilitySync.tsx"),d=r("db/index.ts"),n=r("app/admin/page.tsx");
test("observability profiles events and rehearsals are durable and indexed",()=>{for(const x of["integration_observability_profiles","integration_observability_events","integration_observability_rehearsals"])assert.match(s,new RegExp(x));for(const x of["uq_integration_observability_reference","idx_integration_observability_posture","idx_integration_observability_events","idx_integration_observability_rehearsal"])assert.match(s,new RegExp(x))});
test("opaque references are hashed and telemetry material rejected",()=>{assert.match(l,/crypto\.subtle\.digest\("SHA-256"/);assert.match(s,/signalReferenceHash/);assert.match(l,/never a log, payload, query, endpoint, identity, contact, or credential/);assert.doesNotMatch(s,/logBody|payloadBody|queryText|endpointUrl|contactEmail/)});
test("workspace omits signal reference hashes",()=>{assert.match(l,/select\(\{id:integrationObservabilityProfiles\.id,signalClass:/);assert.doesNotMatch(l.match(/getIntegrationObservabilityWorkspace[\s\S]*?return\{profiles/)?.[0]??"",/signalReferenceHash:/)});
test("signals coverage correlation redaction retention and ownership are bounded",()=>{for(const x of["reconciliation_signal","data_quality_signal","synthetic_coverage","synthetic_correlation_verified","synthetic_redaction_verified","approved_schedule_referenced","primary_and_backup_named","synthetic_tested","synthetic_rehearsed"])assert.match(l,new RegExp(x))});
test("profile review is optimistic and independent",()=>{assert.match(l,/ne\(integrationObservabilityProfiles\.createdByUserId,u\)/);assert.match(l,/IntegrationObservabilityConflictError/);assert.doesNotMatch(l,/\.delete\(/)});
test("all observability runtime effects are centrally disabled",()=>{for(const x of["integrationObservabilityTelemetryCollection","integrationObservabilityPayloadInspection","integrationObservabilityExternalTelemetryExport","integrationObservabilityAutomatedAlertDelivery","integrationObservabilityExternalContact"])assert.equal(f.includes(`${x}: false`),true)});
test("forty eight scenario rehearsal has zero effects",()=>{assert.match(l,/scenarioCount:48/);for(const x of["telemetryCollected:0","payloadsInspected:0","exportsStarted:0","pagesSent:0","externalSystemsContacted:0"])assert.match(l,new RegExp(x))});
test("API is private authenticated rate limited and conflict aware",()=>{assert.match(a,/getOrCreateCurrentUser/);assert.match(a,/enforceWriteRateLimit/);assert.match(a,/private, no-store/);assert.match(a,/rateLimitResponse/)});
test("security auditor receives aggregate posture only",()=>{assert.match(l,/platform_admin",\s*"security_auditor/);assert.match(l,/aggregateOnly:true/);assert.doesNotMatch(g,/signalReferenceHash|createdByUserId|profileId/)});
test("bilingual operations and governance surfaces recover safely",()=>{assert.match(p,/useReyatiLocale/);assert.match(p,/Retry/);assert.match(p,/Never enter logs or payloads/);assert.match(g,/useReyatiLocale/);assert.match(g,/Retry/);assert.match(g,/No telemetry data/)});
test("schema capability navigation and bilingual titles are integrated",()=>{assert.match(d,/integration-observability-schema/);assert.match(d,/\.\.\.integrationObservabilitySchema/);assert.match(c,/integration_observability_trace_governance/);for(const x of["/admin/integration-observability","/admin/integration-observability-governance"])assert.equal(t.split(x).length>=3,true);assert.match(n,/\/admin\/integration-observability/)});
